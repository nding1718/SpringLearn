# LDAP server can use LDIF(LDAP Data interexchange Format) files to exchage user data
# this line will allow Spring boot pulls in an LDIF data files
spring.ldap.embedded.ldif=classpath:test-server.ldif
spring.ldap.embedded.base-dn=dc=springframework,dc=org
spring.ldap.embedded.port=8389